<Page x:Class="Diploma2.Views.CostDiagram"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Diploma2.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CostDiagram"
Background="White"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">

  <Page.Resources>
    <!--<ResourceDictionary Source="../Resources/Styles.xaml" />-->
  </Page.Resources>

  <Grid>


    <lvc:CartesianChart Grid.Column="0"
                        Name="Chart"
                        Series="{Binding SeriesCollection, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, IsAsync=True}"
                        LegendLocation="Bottom">

      <!--<lvc:CartesianChart.AxisX>
        <lvc:Axis Title="simulationIndex"
                  Labels="{Binding Labels}" />
      </lvc:CartesianChart.AxisX>

      <lvc:CartesianChart.AxisY>
        <lvc:Axis Title="Cost"
                  LabelFormatter="{Binding Formatter}"
                  MinValue="0"
                  MaxValue="20"/>
      </lvc:CartesianChart.AxisY>-->

    </lvc:CartesianChart>

    
  </Grid>
</Page>